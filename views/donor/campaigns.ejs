<!DOCTYPE html>
<html lang="en" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campaigns - RaktSetu</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ef4444;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
        }

        .nav-link {
            color: #4b5563;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
        }

        .nav-link:hover {
            background-color: rgba(239, 68, 68, 0.05);
            color: var(--primary);
        }

        .nav-link.active {
            color: var(--primary);
            font-weight: 600;
            background-color: rgba(239, 68, 68, 0.1);
        }
    </style>
</head>

<body class="min-h-screen bg-gray-50">
    <% var _user=(typeof user !=='undefined' ) ? user : { fullName: 'Donor' , _id: '' }; %>
        <nav class="bg-white shadow-sm sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center">
                        <div class="flex-shrink-0 flex items-center">
                            <i class="fas fa-tint text-red-600 text-2xl mr-2"></i>
                            <span class="text-xl font-bold text-gray-800">RaktSetu</span>
                        </div>
                        <div class="hidden md:ml-10 md:flex md:space-x-1">
                            <a href="/donor-dashboard/<%= _user._id %>/dashboard" class="nav-link">Dashboard</a>
                            <a href="/donor-dashboard/<%= _user._id %>/requests" class="nav-link relative">
                                Requests
                                <% if (typeof pendingCount !=='undefined' && pendingCount> 0) { %>
                                    <span class="absolute top-1 right-1 flex h-2.5 w-2.5">
                                        <span
                                            class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                                        <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
                                    </span>
                                    <% } %>
                            </a>
                            <a href="/donor-dashboard/<%= _user._id %>/history" class="nav-link">History</a>
                            <a href="/donor-dashboard/<%= _user._id %>/campaigns" class="nav-link active">Campaigns</a>
                            <a href="/donor-dashboard/<%= _user._id %>/wellness" class="nav-link">Wellness</a>
                        </div>
                    </div>
                    <div class="hidden md:flex items-center space-x-4">
                        <span class="text-sm font-medium text-gray-700">
                            <%= _user.fullName %>
                        </span>
                        <a href="/get-started" class="text-gray-500 hover:text-red-600 ml-4"><i
                                class="fas fa-sign-out-alt"></i></a>
                    </div>
                </div>
            </div>
        </nav>
        <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">Active Campaigns & Drives</h2>
            <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
                <!-- Campaign Card 1 -->
                <div
                    class="bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-md transition-shadow border border-gray-100">
                    <div class="h-40 bg-gradient-to-r from-red-500 to-red-600 relative p-6 flex items-end">
                        <h3 class="text-white font-bold text-xl relative z-10">Winter Blood Drive</h3>
                        <div class="absolute inset-0 bg-black opacity-10"></div>
                    </div>
                    <div class="p-6">
                        <div class="flex items-center text-sm text-gray-500 mb-4">
                            <i class="fas fa-map-marker-alt mr-2 text-red-500"></i>
                            <span>Community Center, <%= _user.city || 'City' %></span>
                        </div>
                        <div class="flex items-center text-sm text-gray-500 mb-6">
                            <i class="far fa-calendar mr-2 text-red-500"></i>
                            <span>15-20 Jan, 2024 • 9:00 AM - 5:00 PM</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-xs font-semibold bg-red-50 text-red-600 px-2 py-1 rounded">Urgent</span>
                            <button
                                class="bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-700">Join
                                Now</button>
                        </div>
                    </div>
                </div>

                <!-- Campaign Card 2 -->
                <div
                    class="bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-md transition-shadow border border-gray-100">
                    <div class="h-40 bg-gradient-to-r from-blue-500 to-indigo-600 relative p-6 flex items-end">
                        <h3 class="text-white font-bold text-xl relative z-10">Corporate Drive</h3>
                        <div class="absolute inset-0 bg-black opacity-10"></div>
                    </div>
                    <div class="p-6">
                        <div class="flex items-center text-sm text-gray-500 mb-4">
                            <i class="fas fa-map-marker-alt mr-2 text-blue-500"></i>
                            <span>Tech Park, Sector 62</span>
                        </div>
                        <div class="flex items-center text-sm text-gray-500 mb-6">
                            <i class="far fa-calendar mr-2 text-blue-500"></i>
                            <span>25 Jan, 2024 • 10:00 AM - 4:00 PM</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-xs font-semibold bg-blue-50 text-blue-600 px-2 py-1 rounded">Open</span>
                            <button
                                class="bg-white border border-gray-200 text-gray-700 px-4 py-2 rounded-lg text-sm font-medium hover:bg-gray-50">Details</button>
                        </div>
                    </div>
                </div>
            </div>
        </main>
</body>

</html>