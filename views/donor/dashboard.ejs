<!DOCTYPE html>
<html lang="en" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donor Dashboard - RaktSetu</title>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ef4444;
            --primary-light: #fecaca;
            --secondary: #1e40af;
            --dark: #1f2937;
            --light: #f9fafb;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .glass-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.05), 0 4px 6px -2px rgba(0, 0, 0, 0.03);
        }

        .nav-link {
            position: relative;
            transition: color 0.3s ease;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            color: #4b5563;
            /* Default gray-600 */
        }

        .nav-link:hover {
            background-color: rgba(239, 68, 68, 0.05);
            color: var(--primary);
        }

        .nav-link.active {
            color: var(--primary);
            font-weight: 600;
            background-color: rgba(239, 68, 68, 0.1);
        }
    </style>
</head>

<body class="min-h-screen bg-gray-50">
    <% var _user=(typeof user !=='undefined' ) ? user : { fullName: 'Donor' , _id: '' , bloodGroup: 'O+' ,
        city: 'New Delhi' , contactInfo: '+91 XXXXXXXXXX' , lastDonation: '2023-10-15' , totalDonations: 5,
        nextDonationEligible: '2024-01-15' }; %>

        <nav class="bg-white shadow-sm sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center">
                        <div class="flex-shrink-0 flex items-center">
                            <i class="fas fa-tint text-red-600 text-2xl mr-2"></i>
                            <span class="text-xl font-bold text-gray-800">RaktSetu</span>
                        </div>
                        <div class="hidden md:ml-10 md:flex md:space-x-1">
                            <a href="/donor-dashboard/<%= _user._id %>/dashboard" class="nav-link active">
                                <i class="fas fa-tachometer-alt mr-2"></i>Dashboard
                            </a>
                            <a href="/donor-dashboard/<%= _user._id %>/requests" class="nav-link relative">
                                <i class="fas fa-hand-holding-heart mr-2"></i>Requests
                                <% if (typeof pendingCount !=='undefined' && pendingCount> 0) { %>
                                    <span class="absolute top-1 right-1 flex h-2.5 w-2.5">
                                        <span
                                            class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                                        <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
                                    </span>
                                    <% } %>
                            </a>
                            <a href="/donor-dashboard/<%= _user._id %>/history" class="nav-link">
                                <i class="fas fa-history mr-2"></i>History
                            </a>
                            <a href="/donor-dashboard/<%= _user._id %>/campaigns" class="nav-link">
                                <i class="fas fa-calendar-alt mr-2"></i>Campaigns
                            </a>
                            <a href="/donor-dashboard/<%= _user._id %>/wellness" class="nav-link">
                                <i class="fas fa-heartbeat mr-2"></i>Wellness
                            </a>
                        </div>
                    </div>

                    <!-- Right Side Profile/Nav -->
                    <div class="hidden md:flex items-center space-x-4">
                        <!-- Notifications (Simplified) -->
                        <button class="p-2 rounded-full text-gray-500 hover:text-red-600 transition-colors relative">
                            <i class="fas fa-bell text-xl"></i>
                            <span
                                class="absolute top-1 right-1 h-2.5 w-2.5 bg-red-500 rounded-full border-2 border-white"></span>
                        </button>

                        <div class="flex items-center space-x-2">
                            <img class="h-8 w-8 rounded-full border border-gray-200"
                                src="https://ui-avatars.com/api/?name=<%= _user.fullName %>&background=ef4444&color=fff"
                                alt="Profile">
                            <span class="text-sm font-medium text-gray-700">
                                <%= _user.fullName %>
                            </span>
                        </div>

                        <a href="/get-started" class="text-gray-500 hover:text-red-600 ml-4" title="Sign Out">
                            <i class="fas fa-sign-out-alt text-lg"></i>
                        </a>
                    </div>
                </div>
            </div>
        </nav>

        <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
            <!-- Welcome Banner -->
            <div
                class="bg-gradient-to-r from-red-600 to-red-500 rounded-2xl p-8 text-white mb-8 shadow-lg relative overflow-hidden">
                <div class="absolute top-0 right-0 -mt-10 -mr-10 h-64 w-64 bg-white opacity-10 rounded-full blur-3xl">
                </div>
                <div class="relative z-10 flex flex-col md:flex-row md:items-center md:justify-between">
                    <div>
                        <h2 class="text-3xl font-bold mb-2">Welcome back, <%= _user.fullName %>! 👋</h2>
                        <p class="text-red-100 text-lg">Your blood type <span
                                class="font-bold bg-white/20 px-2 py-0.5 rounded ml-1">
                                <%= _user.bloodGroup %>
                            </span> is currently in high demand.</p>
                    </div>
                    <div class="mt-6 md:mt-0">
                        <div
                            class="flex items-center bg-white/10 backdrop-blur-sm px-4 py-2 rounded-lg border border-white/20">
                            <span class="relative flex h-3 w-3 mr-3">
                                <span
                                    class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                                <span class="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span>
                            </span>
                            <span class="font-medium">Active Donor Status</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Stats Overview -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                <div class="glass-card rounded-2xl p-6 relative overflow-hidden group">
                    <div
                        class="absolute -right-6 -bottom-6 text-9xl text-gray-50 opacity-5 group-hover:opacity-10 transition-opacity rotate-12">
                        <i class="fas fa-tint"></i>
                    </div>
                    <div class="flex items-start justify-between">
                        <div>
                            <p class="text-sm font-semibold text-gray-500 uppercase tracking-wide">Blood Group</p>
                            <h3 class="text-3xl font-bold text-gray-900 mt-2">
                                <%= _user.bloodGroup %>
                            </h3>
                        </div>
                        <div class="p-3 bg-red-50 rounded-xl text-red-500">
                            <i class="fas fa-tint text-xl"></i>
                        </div>
                    </div>
                </div>

                <div class="glass-card rounded-2xl p-6 relative overflow-hidden group">
                    <div
                        class="absolute -right-6 -bottom-6 text-9xl text-gray-50 opacity-5 group-hover:opacity-10 transition-opacity rotate-12">
                        <i class="fas fa-hand-holding-heart"></i>
                    </div>
                    <div class="flex items-start justify-between">
                        <div>
                            <p class="text-sm font-semibold text-gray-500 uppercase tracking-wide">Total Donations</p>
                            <h3 class="text-3xl font-bold text-gray-900 mt-2">
                                <%= typeof totalDonations !=='undefined' ? totalDonations : (_user.totalDonations || 0)
                                    %>
                            </h3>
                        </div>
                        <div class="p-3 bg-blue-50 rounded-xl text-blue-500">
                            <i class="fas fa-hand-holding-heart text-xl"></i>
                        </div>
                    </div>
                </div>

                <div class="glass-card rounded-2xl p-6 relative overflow-hidden group">
                    <div
                        class="absolute -right-6 -bottom-6 text-9xl text-gray-50 opacity-5 group-hover:opacity-10 transition-opacity rotate-12">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                    <div class="flex items-start justify-between">
                        <div>
                            <p class="text-sm font-semibold text-gray-500 uppercase tracking-wide">Last Donation</p>
                            <div
                                class="flex items-center mt-2 group-hover:bg-gray-50 rounded-lg -ml-2 pl-2 pr-1 py-1 transition-colors w-fit">
                                <h3 id="displayLastDonation" class="text-xl font-bold text-gray-900 truncate mr-2">
                                    <% let finalDate=typeof lastDonationDate !=='undefined' ? lastDonationDate :
                                        _user.lastDonation; let dateStr='N/A' ; if (finalDate) { const d=new
                                        Date(finalDate); if (!isNaN(d.getTime())) {
                                        dateStr=d.toLocaleDateString('en-IN', {day: 'numeric' , month: 'short' ,
                                        year: 'numeric' }); } } %>
                                        <%= dateStr %>
                                </h3>
                                <button onclick="openDateModal()"
                                    class="text-gray-400 hover:text-red-500 transition-colors bg-white hover:bg-red-50 p-1.5 rounded-full shadow-sm border border-gray-100 opacity-0 group-hover:opacity-100">
                                    <i class="fas fa-pencil-alt text-xs"></i>
                                </button>
                            </div>
                        </div>
                        <div class="p-3 bg-yellow-50 rounded-xl text-yellow-600">
                            <i class="fas fa-calendar-check text-xl"></i>
                        </div>
                    </div>
                </div>

                <div class="glass-card rounded-2xl p-6 relative overflow-hidden group">
                    <div
                        class="absolute -right-6 -bottom-6 text-9xl text-gray-50 opacity-5 group-hover:opacity-10 transition-opacity rotate-12">
                        <i class="fas fa-heartbeat"></i>
                    </div>
                    <div class="flex items-start justify-between">
                        <div>
                            <p class="text-sm font-semibold text-gray-500 uppercase tracking-wide">Next Eligible</p>
                            <h3 class="text-xl font-bold mt-2 truncate <% 
                                let nDate = typeof nextEligibleDate !== 'undefined' ? nextEligibleDate : _user.nextDonationEligible;
                                let isEligibleNow = !nDate || new Date(nDate) <= new Date();
                             %> <%= isEligibleNow ? 'text-green-600' : 'text-gray-700' %>">
                                <% let eText='Now' ; if (nDate) { const d=new Date(nDate); const now=new Date(); if
                                    (!isNaN(d.getTime()) && d> now) {
                                    eText = d.toLocaleDateString('en-IN', {day: 'numeric', month: 'short', year:
                                    'numeric'});
                                    }
                                    }
                                    %>
                                    <span
                                        title="<%= eText === 'Now' ? 'You are eligible to donate!' : 'Eligible on ' + eText %>">
                                        <%= eText %>
                                    </span>
                            </h3>
                        </div>
                        <div class="p-3 bg-green-50 rounded-xl text-green-600">
                            <i class="fas fa-heartbeat text-xl"></i>
                        </div>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Left Column: Quick Profile -->
                <div class="md:col-span-1 space-y-6">
                    <div class="glass-card rounded-2xl p-6">
                        <div class="flex flex-col items-center text-center">
                            <div class="relative">
                                <img class="h-28 w-28 rounded-full border-4 border-white shadow-lg mb-4"
                                    src="https://ui-avatars.com/api/?name=<%= _user.fullName %>&background=ef4444&color=fff&size=200"
                                    alt="Profile">
                                <div
                                    class="absolute bottom-4 right-2 h-5 w-5 bg-green-500 border-2 border-white rounded-full">
                                </div>
                            </div>
                            <h3 class="text-xl font-bold text-gray-900">
                                <%= _user.fullName %>
                            </h3>
                            <p class="text-sm text-gray-500 mb-4">
                                <%= _user.city %>, <%= _user.state || 'India' %>
                            </p>

                            <div class="w-full border-t border-gray-100 pt-4 mt-2">
                                <div class="flex justify-between items-center py-2 text-sm">
                                    <span class="text-gray-500">Mobile</span>
                                    <span class="font-medium text-gray-900">
                                        <%= _user.contactInfo %>
                                    </span>
                                </div>
                                <div class="flex justify-between items-center py-2 text-sm">
                                    <span class="text-gray-500">Email</span>
                                    <span class="font-medium text-gray-900 truncate max-w-[150px]"
                                        title="<%= _user.email %>">
                                        <%= _user.email %>
                                    </span>
                                </div>
                            </div>

                            <button
                                class="w-full mt-6 py-2.5 bg-gray-900 hover:bg-gray-800 text-white rounded-xl text-sm font-medium transition-colors shadow-lg shadow-gray-200">
                                Edit Profile
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Right Column: Quick Stats / Actions -->
                <div class="md:col-span-2 space-y-6">
                    <!-- Action Card -->
                    <div class="glass-card rounded-2xl p-8 border-l-4 border-l-red-500">
                        <div class="flex flex-col md:flex-row items-center justify-between">
                            <div class="mb-6 md:mb-0">
                                <h3 class="text-xl font-bold text-gray-900 mb-2">Ready to save a life?</h3>
                                <p class="text-gray-600 max-w-md">There are patients in your area who urgently need <%=
                                        _user.bloodGroup %> blood. Your donation can make a difference today.</p>
                            </div>
                            <a href="/donor-dashboard/<%= _user._id %>/requests"
                                class="px-6 py-3 bg-red-600 hover:bg-red-700 text-white rounded-xl font-semibold shadow-lg shadow-red-200 transition-transform active:scale-95 flex items-center">
                                View Requests <i class="fas fa-arrow-right ml-2 text-sm"></i>
                            </a>
                        </div>
                    </div>

                    <!-- Tip Card -->
                    <div class="glass-card rounded-2xl p-6 bg-gradient-to-br from-blue-50 to-white border-blue-100">
                        <div class="flex items-start">
                            <div class="p-3 bg-blue-100 rounded-lg text-blue-600 mr-4">
                                <i class="fas fa-info-circle text-xl"></i>
                            </div>
                            <div>
                                <h4 class="font-bold text-gray-900 mb-1">Did you know?</h4>
                                <p class="text-sm text-gray-600">A single donation can save up to 3 lives. Your past
                                    donations have potentially impacted over <span class="font-bold text-blue-600">
                                        <%= (typeof totalDonations !=='undefined' ? totalDonations :
                                            (_user.totalDonations || 0)) * 3 %> people
                                    </span>. Keep up the great work!</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <script>
            // Any specific dashboard scripts can go here

            const modal = document.getElementById('dateModal');
            const donorId = '<%= _user._id %>';

            function openDateModal() {
                modal.classList.remove('hidden');
            }

            function closeDateModal() {
                modal.classList.add('hidden');
            }

            async function saveDate() {
                const date = document.getElementById('newLastDonation').value;
                const btn = document.querySelector('#dateModal button.bg-red-600');

                if (!date) return alert('Please select a date');

                btn.disabled = true;
                btn.innerHTML = '<i class="fas fa-circle-notch fa-spin"></i> Saving...';

                try {
                    const res = await fetch(`/api/donor/${donorId}/update-donation`, {
                        method: 'PUT',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ lastDonation: date })
                    });

                    const data = await res.json();

                    if (data.success) {
                        location.reload(); // Reload to update all stats server-side
                    } else {
                        alert(data.message || 'Failed to update');
                    }
                } catch (err) {
                    console.error(err);
                    alert('Server error');
                } finally {
                    btn.disabled = false;
                    btn.innerHTML = 'Save Changes';
                    closeDateModal();
                }
            }
        </script>

        <!-- Update Date Modal -->
        <div id="dateModal" class="fixed inset-0 z-[100] hidden" aria-labelledby="modal-title" role="dialog"
            aria-modal="true">
            <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity" onclick="closeDateModal()"></div>
            <div class="fixed inset-0 z-10 overflow-y-auto">
                <div class="flex min-h-full items-end justify-center p-4 text-center sm:items-center sm:p-0">
                    <div
                        class="relative transform overflow-hidden rounded-lg bg-white text-left shadow-xl transition-all sm:my-8 sm:w-full sm:max-w-lg">
                        <div class="bg-white px-4 pb-4 pt-5 sm:p-6 sm:pb-4">
                            <div class="sm:flex sm:items-start">
                                <div
                                    class="mx-auto flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-full bg-red-100 sm:mx-0 sm:h-10 sm:w-10">
                                    <i class="fas fa-calendar-alt text-red-600"></i>
                                </div>
                                <div class="mt-3 text-center sm:ml-4 sm:mt-0 sm:text-left w-full">
                                    <h3 class="text-base font-semibold leading-6 text-gray-900" id="modal-title">Update
                                        Last Donation</h3>
                                    <div class="mt-2">
                                        <p class="text-sm text-gray-500 mb-4">Please enter the date of your last
                                            successful blood donation. Your eligibility will be recalculated.</p>
                                        <input type="date" id="newLastDonation"
                                            class="block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-red-600 sm:text-sm sm:leading-6">
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="bg-gray-50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6">
                            <button type="button" onclick="saveDate()"
                                class="inline-flex w-full justify-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 sm:ml-3 sm:w-auto">Save
                                Changes</button>
                            <button type="button" onclick="closeDateModal()"
                                class="mt-3 inline-flex w-full justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50 sm:mt-0 sm:w-auto">Cancel</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</body>

</html>