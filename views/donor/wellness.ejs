<!DOCTYPE html>
<html lang="en" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wellness - RaktSetu</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ef4444;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
        }

        .nav-link {
            color: #4b5563;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
        }

        .nav-link:hover {
            background-color: rgba(239, 68, 68, 0.05);
            color: var(--primary);
        }

        .nav-link.active {
            color: var(--primary);
            font-weight: 600;
            background-color: rgba(239, 68, 68, 0.1);
        }
    </style>
</head>

<body class="min-h-screen bg-gray-50">
    <% var _user=(typeof user !=='undefined' ) ? user : { fullName: 'Donor' , _id: '' }; %>
        <nav class="bg-white shadow-sm sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center">
                        <div class="flex-shrink-0 flex items-center">
                            <i class="fas fa-tint text-red-600 text-2xl mr-2"></i>
                            <span class="text-xl font-bold text-gray-800">RaktSetu</span>
                        </div>
                        <div class="hidden md:ml-10 md:flex md:space-x-1">
                            <a href="/donor-dashboard/<%= _user._id %>/dashboard" class="nav-link">Dashboard</a>
                            <a href="/donor-dashboard/<%= _user._id %>/requests" class="nav-link relative">
                                Requests
                                <% if (typeof pendingCount !=='undefined' && pendingCount> 0) { %>
                                    <span class="absolute top-1 right-1 flex h-2.5 w-2.5">
                                        <span
                                            class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                                        <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-red-500"></span>
                                    </span>
                                    <% } %>
                            </a>
                            <a href="/donor-dashboard/<%= _user._id %>/history" class="nav-link">History</a>
                            <a href="/donor-dashboard/<%= _user._id %>/campaigns" class="nav-link">Campaigns</a>
                            <a href="/donor-dashboard/<%= _user._id %>/wellness" class="nav-link active">Wellness</a>
                        </div>
                    </div>
                    <div class="hidden md:flex items-center space-x-4">
                        <span class="text-sm font-medium text-gray-700">
                            <%= _user.fullName %>
                        </span>
                        <a href="/get-started" class="text-gray-500 hover:text-red-600 ml-4"><i
                                class="fas fa-sign-out-alt"></i></a>
                    </div>
                </div>
            </div>
        </nav>
        <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">Health & Wellness</h2>
            <div class="grid gap-6 md:grid-cols-2">
                <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-100 flex items-start">
                    <div
                        class="h-12 w-12 rounded-full bg-green-100 text-green-600 flex items-center justify-center text-xl mr-4 flex-shrink-0">
                        <i class="fas fa-wine-glass"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-gray-900 mb-2">Hydrate Well</h3>
                        <p class="text-gray-600 text-sm">Drink at least 500ml of water before donating. Being hydrated
                            helps maintain your blood volume and prevents dizziness.</p>
                    </div>
                </div>

                <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-100 flex items-start">
                    <div
                        class="h-12 w-12 rounded-full bg-red-100 text-red-600 flex items-center justify-center text-xl mr-4 flex-shrink-0">
                        <i class="fas fa-apple-alt"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-gray-900 mb-2">Iron-Rich Diet</h3>
                        <p class="text-gray-600 text-sm">Consume iron-rich foods like spinach, beans, and lean meats in
                            the weeks leading up to your donation.</p>
                    </div>
                </div>

                <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-100 flex items-start">
                    <div
                        class="h-12 w-12 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center text-xl mr-4 flex-shrink-0">
                        <i class="fas fa-bed"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-gray-900 mb-2">Rest Up</h3>
                        <p class="text-gray-600 text-sm">Get a good night's sleep before your donation day. Avoid heavy
                            lifting or strenuous exercise immediately after donating.</p>
                    </div>
                </div>
            </div>
        </main>
</body>

</html>