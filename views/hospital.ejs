<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Dashboard - RaktSetu</title>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8fafc;
        }
        .glass-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        .nav-link {
            position: relative;
            transition: color 0.3s ease;
        }
        .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -4px;
            left: 0;
            background-color: #1d4ed8;
            transition: width 0.3s ease;
        }
        .nav-link:hover::after,
        .nav-link.active::after {
            width: 100%;
        }
        .nav-link.active {
            color: #1d4ed8;
            font-weight: 500;
        }
    </style>
</head>
<body class="min-h-screen">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <div class="flex-shrink-0 flex items-center">
                        <i class="fas fa-hospital text-blue-600 text-2xl mr-2"></i>
                        <span class="text-xl font-bold text-gray-800">RaktSetu - Hospital</span>
                    </div>
                    <div class="hidden md:ml-10 md:flex md:space-x-8">
                        <a href="/hospital/<%= user._id %>" class="nav-link active text-gray-900">Dashboard</a>
                        <a href="/hospital/<%= user._id %>/requests" class="nav-link text-gray-500 hover:text-gray-700">Requests</a>
                        <a href="#" class="nav-link text-gray-500 hover:text-gray-700">Donors</a>
                        <a href="#" class="nav-link text-gray-500 hover:text-gray-700">Reports</a>
                    </div>
                </div>
                <div class="hidden md:ml-4 md:flex md:items-center">
                    <div class="relative" x-data="{ open: false }">
                        <button @click="open = !open" class="flex items-center max-w-xs rounded-full focus:outline-none">
                            <span class="ml-2 text-sm font-medium text-gray-700"><%= user.hospitalName %></span>
                            <i class="fas fa-chevron-down ml-1 text-gray-500 text-xs"></i>
                        </button>
                        <div x-show="open" @click.away="open = false" class="origin-top-right absolute right-0 mt-2 w-48 rounded-md shadow-lg bg-white ring-1 ring-black ring-opacity-5">
                            <div class="py-1">
                                <a href="#" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Settings</a>
                                <a href="#" onclick="deleteAccount('<%= user._id %>')" class="block px-4 py-2 text-sm text-red-700 hover:bg-gray-100">Delete Account</a>
                                <a href="/get-started" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Sign out</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6" x-data="{ showLocationModal: false }">
        <h1 class="text-3xl font-bold text-gray-900 mb-6">Dashboard</h1>

        <!-- Blood Stock Overview -->
        <div class="mb-8">
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Blood Stock Overview</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-8 gap-4 text-center">
                <% const bloodGroups = ['A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-']; %>
                <% bloodGroups.forEach(group => { %>
                    <div class="bg-white p-4 rounded-lg shadow">
                        <div class="text-2xl font-bold text-red-600"><%= group %></div>
                        <div class="text-lg font-medium text-gray-700">
                            <%= user.bloodStock[group] || 0 %> 
                            <span class="text-sm text-gray-500">units</span>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>

        <!-- Hospital Details -->
        <div class="glass-card rounded-xl p-6 mb-8">
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Hospital Details</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <p class="text-sm font-medium text-gray-500">Address</p>
                    <p class="text-gray-800"><%= user.address %>, <%= user.city %>, <%= user.pincode %></p>
                </div>
                <div>
                    <p class="text-sm font-medium text-gray-500">Coordinates</p>
                    <div class="flex items-center gap-4">
                        <% if (user.location && user.location.coordinates && user.location.coordinates.length === 2) { %>
                            <p class="text-gray-800"><%= user.location.coordinates[1] %>, <%= user.location.coordinates[0] %></p>
                        <% } else { %>
                            <p class="text-gray-800">Not set</p>
                        <% } %>
                        
                        <% if (user.locationUpdateAttempts === undefined || user.locationUpdateAttempts > 0) { %>
                        <button @click="showLocationModal = true" class="bg-blue-500 text-white px-3 py-1 rounded-md text-sm hover:bg-blue-600">
                            <%= (user.location && user.location.coordinates && user.location.coordinates.length === 2) ? 'Update Location' : 'Set Location' %>
                        </button>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Update Blood Stock -->
        <div class="glass-card rounded-xl p-6 mb-8">
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Update Blood Stock</h2>
            <form id="updateStockForm" class="flex flex-wrap items-center gap-4">
                <div class="flex-grow">
                    <label for="bloodGroup" class="sr-only">Blood Group</label>
                    <select id="bloodGroup" name="bloodGroup" class="w-full p-2 border border-gray-300 rounded-md">
                        <% bloodGroups.forEach(group => { %>
                            <option value="<%= group %>"><%= group %></option>
                        <% }); %>
                    </select>
                </div>
                <div class="flex-grow">
                    <label for="units" class="sr-only">Units</label>
                    <input type="number" id="units" name="units" placeholder="Units (e.g., 5 or -2)" class="w-full p-2 border border-gray-300 rounded-md" required>
                </div>
                <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Update Stock</button>
            </form>
        </div>
>>>>>>> Stashed changes
        <!-- Location Modal -->
        <div x-show="showLocationModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full flex items-center justify-center" style="display: none;">
            <div @click.away="showLocationModal = false" class="relative mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
                <div class="mt-3 text-center">
                    <h3 class="text-lg leading-6 font-medium text-gray-900">Set Hospital Location</h3>
                    <div class="mt-2 px-7 py-3">
                        <p class="text-sm text-gray-500">
                            Click the button below to use your device's current location. You have <%= user.locationUpdateAttempts === undefined ? 3 : user.locationUpdateAttempts %> attempts remaining.
                        </p>
                        <div id="location-info" class="mt-4 text-gray-700"></div>
                    </div>
                    <div class="items-center px-4 py-3">
                        <button id="getLocationBtn" class="px-4 py-2 bg-blue-500 text-white text-base font-medium rounded-md w-full shadow-sm hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-300">
                            Get Live Location
                        </button>
                        <button id="saveLocationBtn" class="mt-2 px-4 py-2 bg-green-500 text-white text-base font-medium rounded-md w-full shadow-sm hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-300" style="display: none;">
                            Save Location
                        </button>
                    </div>
                    <div class="items-center px-4 py-3">
                        <button @click="showLocationModal = false" class="px-4 py-2 bg-gray-200 text-gray-800 text-base font-medium rounded-md w-full shadow-sm hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-300">
                            Cancel
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script>
        document.getElementById('updateStockForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const bloodGroup = e.target.bloodGroup.value;
            const units = e.target.units.value;
            const hospitalId = '<%= user._id %>';

            try {
                const response = await fetch(`/api/stock/update/${hospitalId}`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ bloodGroup, units: parseInt(units) })
                });
                if (response.ok) {
                    alert('Stock updated successfully!');
                    location.reload();
                } else {
                    alert('Failed to update stock.');
                }
            } catch (error) {
                console.error('Error updating stock:', error);
                alert('An error occurred.');
            }
        });

        async function deleteAccount(hospitalId) {
            if (confirm('Are you sure you want to delete your account? This action cannot be undone.')) {
                try {
                    const response = await fetch(`/api/hospital/${hospitalId}`, {
                        method: 'DELETE'
                    });
                    if (response.ok) {
                        alert('Account deleted successfully.');
                        window.location.href = '/get-started';
                    } else {
                        alert('Failed to delete account.');
                    }
                } catch (error) {
                    console.error('Error deleting account:', error);
                    alert('An error occurred.');
                }
            }
        }

        let currentCoordinates = null;

        document.addEventListener('DOMContentLoaded', () => {
            const getLocationBtn = document.getElementById('getLocationBtn');
            const saveLocationBtn = document.getElementById('saveLocationBtn');
            const locationInfo = document.getElementById('location-info');

            if(getLocationBtn) {
                getLocationBtn.addEventListener('click', () => {
                    if (navigator.geolocation) {
                        navigator.geolocation.getCurrentPosition(position => {
                            const lat = position.coords.latitude;
                            const lon = position.coords.longitude;
                            currentCoordinates = [lon, lat];
                            locationInfo.innerHTML = `Latitude: ${lat.toFixed(4)}, Longitude: ${lon.toFixed(4)}`;
                            if(saveLocationBtn) saveLocationBtn.style.display = 'block';
                        }, error => {
                            alert(`Error: ${error.message}`);
                        });
                    } else {
                        alert('Geolocation is not supported by this browser.');
                    }
                });
            }

            if(saveLocationBtn) {
                saveLocationBtn.addEventListener('click', async () => {
                    if (!currentCoordinates) {
                        alert('No location data to save.');
                        return;
                    }
                    const hospitalId = '<%= user._id %>';
                    try {
                        const response = await fetch(`/api/hospital/location/${hospitalId}`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ coordinates: currentCoordinates })
                        });
                        if (response.ok) {
                            alert('Location updated successfully!');
                            location.reload();
                        } else {
                            const errorData = await response.json().catch(() => ({ message: 'An unknown error occurred.' }));
                            alert(`Failed to update location: ${errorData.message}`);
                        }
                    } catch (error) {
                        console.error('Error updating location:', error);
                        alert('An error occurred while updating location.');
                    }
                });
            }
        });
    </script>
</body>
</html>
